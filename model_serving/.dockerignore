# Virtual environments and caches
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# Logs and runtime artifacts
logs/
*.log
mlflow.db
mlflow.db-*

# Remote models (downloaded inside the image at build-time)
models/remote/

# Tests (not needed in production image; CI runs them separately)
tests/

# Git metadata
.git/
.gitignore

# Docker compose (not needed inside image)
docker-compose.yml

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp

# Legacy directories (migrated to models/local/)
models_local/
preprocessors/
